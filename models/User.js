class User {
  constructor(
    id,
    firstName,
    secondName,
    surname,
    role,
    email,
    number,
    birthday,
    password,
    nickname,
  ) {
    this.id = id;
    this.firstName = firstName;
    this.secondName = secondName;
    this.role = role;
    this.email = email;
    this.number = number;
  }
}
